public enum AttackerState
{
    STOPPED = 0,
    ATTACKING,
    STARTED,
}

public enum RangeAttackerState
{
    STOPPED = 0,
    ATTACKING,
    STARTED,
    AIMING,
    RELOADING,
}

public enum MeleeAttackerState
{
    STOPPED = 0,
    ATTACKING,
    STARTED,
}